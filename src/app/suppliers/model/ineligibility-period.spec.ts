import { IneligibilityPeriod } from './ineligibility-period';

describe('IneligibilityPeriod', () => {
  it('should create an instance', () => {
    expect(new IneligibilityPeriod()).toBeTruthy();
  });
});
